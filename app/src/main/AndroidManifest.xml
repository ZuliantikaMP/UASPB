<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uaspb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".About"></activity>
        <activity android:name=".Linkingwords" />
        <activity android:name=".conditionals" />
        <activity android:name=".verb" />
        <activity android:name=".presentperfect" />
        <activity android:name=".futuretense" />
        <activity android:name=".pasttense" />
        <activity android:name=".Noun" />
        <activity android:name=".present" />
        <activity android:name=".Grammer" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>